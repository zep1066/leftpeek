config = {
    easeTime = 100,
    camOffset = vec3(-0.7, 0.0, 0.0),

    keyMapping = {
        name = "leftpeek",
        description = "Toggle Left Peek",
        defaultKey = "g",
        defaultMapper = "keyboard",
    }
}