config = {
    easeTime = 100,
    camOffset = vec3(-0.7, 0.0, 0.0),

    keyMapping = {
        name = "leftpeek",
        description = "Toggle Left Shoulder",
        defaultKey = "g",
        defaultMapper = "keyboard",
    }
}